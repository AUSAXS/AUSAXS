This folder collects the compile-time constants used throughout AUSAXS.

It includes physical constants and SI conversion factors, default axis definitions, atomic properties such as the van der Waals radius table, recognized file extensions, and the build version. Keeping these in one place ensures every subproject agrees on the same values.

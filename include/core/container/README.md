This folder contains lightweight generic container types.

`Container1D`, `Container2D`, and `Container3D` provide simple flat-storage multidimensional arrays used wherever indexed numerical data is needed. `ThreadLocalWrapper` gives each thread its own copy of a value for lock-free accumulation in the multithreaded histogram code.

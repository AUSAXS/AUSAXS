This folder collects general-purpose utilities shared across the codebase that do not belong to any single subproject.

Among them are axis and limit types, exception definitions, logging and console output, multithreading helpers, random-number generation, string utilities, an observer/observable pair, the non-owning `observer_ptr`, and various type traits and concepts.

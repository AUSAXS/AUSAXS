This folder contains the plotting helpers used to produce figures.

Each `Plot*` header renders a specific kind of figure — datasets, scattering profiles, distance histograms, optimization landscapes — while `PlotOptions.h` and `Styles.h` control appearance. `All.h` is a convenience header pulling in every plotter.
